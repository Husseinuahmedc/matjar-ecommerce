/**
 * Shared custom hooks — exported from here for global use.
 * Feature-specific hooks live in their feature folder.
 *
 * Hooks to be added:
 * - useUser()      — Get the current authenticated user (client-side)
 * - useLocale()    — Get the current locale with type safety
 * - useToast()     — Re-export from shadcn/ui toast
 */

export {};
