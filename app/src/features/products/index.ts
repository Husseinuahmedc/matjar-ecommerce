/**
 * Products feature module index.
 * Re-exports all public API of the products feature.
 *
 * Feature folder structure (to be built):
 * features/products/
 * ├── components/       — UI components scoped to this feature
 * │   ├── ProductCard.tsx
 * │   ├── ProductGrid.tsx
 * │   ├── ProductFilters.tsx
 * │   ├── ProductGallery.tsx
 * │   └── AddToCartButton.tsx  (Client Component)
 * ├── hooks/
 * │   └── useProductFilters.ts
 * └── types.ts          — Feature-specific types (if different from shared types)
 */

// Exports will be added as components are built
export {};
