/**
 * Auth feature module index.
 * Re-exports public API of the authentication/authorization feature.
 *
 * Feature folder structure (to be built):
 * features/auth/
 * ├── components/
 * │   ├── LoginForm.tsx        — Email/password login form (Client Component)
 * │   ├── RegisterForm.tsx     — Email/password registration form (Client Component)
 * │   └── AuthProvider.tsx     — Session state provider
 * ├── hooks/
 * │   └── useAuth.ts           — Access session user and tokens
 * └── types.ts                 — Auth-specific types
 */

export {};
