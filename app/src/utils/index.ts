/**
 * Shared pure utility functions — no React dependencies.
 * For UI-related utilities (cn, formatPrice), see lib/utils.ts.
 *
 * Utilities to be added:
 * - generateSlug(title: string): string — for product URLs
 * - truncateText(text: string, max: number): string
 * - isServer(): boolean — check if running server-side
 */

export {};
